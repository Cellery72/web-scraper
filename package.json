{
  "name": "web-scraper",
  "version": "0.0.2",
  "description": "Web Scraper (don't tell people) that generates",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cheerio": "^0.20.0",
    "express": "^4.14.0",
    "jsonfile": "^2.3.1",
    "mkdirp": "^0.5.1",
    "request": "^2.72.0"
  }
}
